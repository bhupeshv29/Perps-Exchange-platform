ws
    public:✅
    depth (in futures send deltas)
    trades
    mark price

    private authenticated:✅
    my orders
    my positions
    my balance

//todo
 - script re write for all
 - root env
 - dockerise each package
 -


//optional
    - repo codebase setup (database)
    - common response function
    - status library
    - integer ticks     ✅
            number
                ↓
            working exchange
                ↓
            integer ticks
                ↓
            production-grade precision



production exchange
    TreeMap
    Heap
    Sorted Set
    RB Tree
    Skip List

how to ensure only market created by admin will be traded (not implementing it)

depth should deltas, and also oderbook is not shorted i think cause it is just using min/max fn


For browser cookie auth later, we’ll parse the cookie from the WS upgrade request. For now, send token via AUTH message.

//Engine = source of truth for active markets
DB = persistence/read history
Admin route = later, sends MARKET_CREATE command to engine

Engine Recovery + // snapshot ✅
candles

testing.


testing(postman) ->


//notes
Three prices you'll usually see
Last Price
    The price of the most recent trade.
    Used for charting and order execution.
Mark Price
    Fair-value price used for PnL and liquidation.
    Usually smoother than the last price.
Index Price
    A weighted average of spot prices from multiple exchanges.
    One of the inputs used to calculate the mark price.
Quick rule
        Orders execute at the market/last price.
        Liquidation happens based on the mark price.
        Unrealized PnL is usually calculated using the mark price.



i should normlly.
convert decimal to int in api layer    ✅



work to do now
    decimal to number at api    ✅
    remove db    ✅
    remove ws    ✅
    complete backend to engine    ✅
    move market in engine from package✅

make conversion into small fn ✅

(just for knowledge) ->
Funding Rate
Cross Margin
Isolated Margin
Reduce Only
Stop Loss
Take Profit
OCO
TP/SL
Maker/Taker Fees
Multi Asset Margin
Portfolio Margin
Multi Engine Sharding
Multi Market Workers
Admin Panel
KYC
Deposits/Withdrawals


Snapshot Engine State✅

apps/engine/src/snapshot/✅
    saveSnapshot.ts✅
    loadSnapshot.ts✅


✔ Orderbook
✔ Matching Engine
✔ Position Engine
✔ Margin Locking
✔ Liquidation
✔ Snapshots
✔ Websocket Updates
✔ Persistence



3. WS events currently send integer ticks. That is okay only if frontend unscales WS payloads too.✅


backend/ wrap in trycatch in onramp

After refactoring files, I’d revisit the liquidation math because that’s where exchange correctness matters most.


( funding Rate
    they all go into processPriceUpdate() instead of bloating the consumer.)

check api or engine market registry be same otherwise move in common package✅

will margin be calculated from leverage or price x quantity



again postman testing (17 june)

{
    "type": "ORDER_ACCEPTED",
    "requestId": "996c2f3a-92f7-49af-a6bc-a7de8c770e12",
    "payload": {
        "order": {
            "id": "2dc923e8-cbb4-41d4-8682-c594028cf298",
            "userId": "f3257764-1e52-44bd-ad6a-d06312423f44",
            "marketId": "BTCUSDT",
            "side": "BID",
            "type": "LIMIT",
            "price": 100000,
            "qty": 1000,
            "filledQty": 0,
            "margin": 100000,
            "leverage": 1,
            "status": "OPEN",
            "createdAt": 1781646262281
        },
        "fills": []
    }
}
should reponse be unscaling ✅

didn't added unscaling for ws booradcasst 
(thinking of either adding in common or not or also moving  market  to commmon)✅



docker add to each http. and make fe in bun, nextjs, tsq, tailwind, and library
margin calulation based on postion value (price x qty) and leverage cal.
orderbook optimization using sorting 
root env 





making frontend


1 remember taking type from common package zod infer✅
2 nextjs, tailwind, shadcn, axios, zustand, tanstack query✅
3 row color not applying✅
4 toaster ✅
5 GET /markets/:marketId/ticker✅
    GET depth
    GET ticker
    then WS updates


That avoids showing “Loading…” forever if a market genuinely has no orders.
For now, the simple if (!depth) is perfectly fine. Later, when you add MarketDataProvider, you can enhance it to use the query’s loading state.


            #charts commit
            Perfect. 🎉

            At this point, your frontend has reached a stage where it actually resembles a real exchange terminal.

            What you’ve built so far ✅

                - Authentication (signin/signup)
                - Trading layout
                - Navbar
                - Market switching
                - Orderbook
                - Trade history
                - TradingView Lightweight Charts
                - Order form
                - Deposit modal
                - Bottom tabs
                - React Query
                - Zustand
                - WebSocket manager
                - Live market updates
                - Live account updates
                - Professional dark theme

            This is already a solid foundation.


next feat ->
        5 stop scrolling behaviour in form fields in order form ✅

        6. Toast notifications✅

        7. Loading skeletons✅

        8. PnL calculation✅

        9. Position liquidation price✅

        10. Responsive layout✅


add tanstack tables
correct depth cal.✅
correct the liquidation cal in backend✅
maintainance margin check✅
self matching stop✅
closed position check✅
order history complete✅

one sided position is creating check in engine✅


3. priceWorker only sends BTCUSDT
4. lastTradePrice has no REST initial value✅


----------------------------------------
            Completed

            Core Engine

            - Orderbook
            - Matching engine
            - Partial fills
            - Market orders
            - Limit orders
            - Self-trade prevention
            - Positions
            - Close position
            - Realized PnL
            - Unrealized PnL
            - Liquidation worker
            - Shared risk calculations
            - Integer engine
            - Scaling layer
            - Binance mark price worker
            - Margin calculated in backend
            - Frontend Order Summary

            This is already a solid exchange core



-------------------------------------------------
next scope
    landing page and resdesign component.
    